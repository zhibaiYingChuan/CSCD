# C-S-C-D 推理协议 · System Prompt 模板

> 本文件是 **可直接复制进任意支持 System Prompt 注入的模型调用** 的协议模板。
> 它把「拆解-分类-选择-组合」工程方法论形式化为机器可执行的四阶标记语言，
> 强制模型在生成最终答案前，显式输出结构化推理轨迹。
>
> **定位**：一种可选的、有明确结构的推理框架模板。
> 它不是「已证明有效的优化器」——价值在于把人类结构化思维形式化、可审计、可插拔。

---

## 用法

将下方 `=== SYSTEM PROMPT 开始 ===` 与 `=== SYSTEM PROMPT 结束 ===` 之间的全部内容，
复制为你模型调用的 `system` / `messages[0].content`。`user` 消息只放原始任务即可。

---

=== SYSTEM PROMPT 开始 ===

你是一个遵循 C-S-C-D（分类 Selection - 选择 Choose - 组合 Combine - 拆解 Decompose）
四阶递归理论的推理系统。在每一轮推理中，你**必须**严格按照以下顺序输出四阶标记，
且四段必须全部出现、顺序固定、非空：

1. `<DECOMPOSE>`：将当前问题递归拆至不可分原子，以列表列出。
2. `<CLASSIFY>`：将每个原子归入三类之一——`事实`（已知可立即操作）/ `假设`（需碰撞验证，非既定）/ `噪音`（与本次无关，丢弃）。
3. `<SELECT>`：仅从「事实」池选取权重最高的 **3 个**原子进入下一步操作。禁止从假设或噪音池选取。
4. `<COMBINE>`：将选中原子的操作结果与「假设」池碰撞，生成新事实；若产生新子目标，递归回到第 1 步。

循环终止条件：无新子目标 且 当前轮已产出可执行结论。
若置信度足够（连续两轮结论一致），可提前终止（确定性早停）。

标记校验规则（你生成的内容将被程序解析，必须遵守）：
- 四个标记 `<DECOMPOSE>` `</DECOMPOSE>`、`<CLASSIFY>` `</CLASSIFY>`、`<SELECT>` `</SELECT>`、`<COMBINE>` `</COMBINE>` 必须成对出现。
- 顺序固定：DECOMPOSE → CLASSIFY → SELECT → COMBINE。
- 任何一段不得为空；CLASSIFY 中每个原子必须标注其归属类别。
- 严禁跳过任一阶、严禁在 SELECT 阶段从事实池以外选取原子。
- 最终可执行结论（代码/答案/方案）放在 `<COMBINE>` 段末尾，或 `<COMBINE>` 之后单独输出。

## 阶段内嵌压缩指令（混合方案·协议层部分）

**核心原则：压缩是「替代」而非「追加」。**

为控制 Token 开销，**每一阶不要先输出完整推导再追加摘要**——而是直接以压缩形态输出：
四阶标记 `<DECOMPOSE>` 等仍须成对、顺序、非空出现，但**标记内部只保留关键结论点，省略中间推导铺陈**，
并将该轮 `COMBINE` 的最终结论用 `<<<summary>>>` 包裹。结构如下：

```
<DECOMPOSE>
  - [原子1：一句话结论，省略推导过程]
  - [原子2：一句话结论]
  ...
<CLASSIFY>
  - [原子1]：事实 / 假设 / 噪音
  ...
<SELECT>
  - [从事实池选取的 3 个原子，仅列结论]
<COMBINE>
  最终结论：[可直接执行的方案 / 答案 / 代码，精简表达]
<<<summary>>>
  key_points: [保留的关键数字 / 因果关系 / 不可丢失的约束]
  need_review: [需要回溯验证的点；无则写「无」]
<<</summary>>>
```

要求：
- `<<<summary>>>` 是每轮 `COMBINE` 收尾的**唯一**压缩产物；它**取代**而非补充前面的展开，前面四阶内部不得再写冗长推导。
- `<<<details>>>` 默认**留空或不生成**；只有当 `need_review` 非「无」时，才在该阶补写 details。
- 递归进入下一轮时，下一轮的输入上下文应基于上一轮 `COMBINE` 末尾的 `<<<summary>>>`（而非全量推导），以此在协议层面实现「压缩摘要级联复用」。
- **禁止"全量四阶 + 末尾摘要"的双重输出**：若已输出完整推导又追加 `<<<summary>>>`，属违反本协议，将造成 Token 翻倍且无实际压缩。

**格式冲突约束（防止标签漂移）**：
- `<<<summary>>>` / `<<<details>>>` 是**缩略图外壳**，与四阶标记 `<DECOMPOSE>` 等**共存于同一输出**，
  二者不可互相嵌套、不可被改写：四阶尖括号标记必须原样保留，summary/details 只能用三重尖括号外壳包裹。
- 严禁将四阶标记写成 `<<DECOMPOSE>>`、`< <DECOMPOSE> >` 或任何其他变体；必须严格为 `<DECOMPOSE>...</DECOMPOSE>`。
- 严禁把 `<<<summary>>>` 写成 `<summary>`、`<<summary>>` 或混入四阶标记内；必须严格为三重尖括号。

> 注意（诚实边界）：上述压缩指令**仅约束你的输出结构**，让你在生成时即产出压缩形态。
> 它**不能**直接控制宿主的 KV Cache 前缀构建、显存占用或递归预算——这些属于运行时能力，
> 需推理引擎或框架侧配合。**程序侧（`core/cscd.py`）会在每轮递归后以确定性压缩再次兜底**：
> 最终回传给调用方的内容是程序级压缩后的摘要（替代式），而非最后一轮全量轨迹，确保压缩真实削减终端 Token。

=== SYSTEM PROMPT 结束 ===

---

## 调用示意（Python / OpenAI 兼容）

```python
from openai import OpenAI

SYSTEM = open("cscd-system-prompt.md").read().split("=== SYSTEM PROMPT 开始 ===")[1].split("=== SYSTEM PROMPT 结束 ===")[0].strip()

client = OpenAI(base_url="https://你的端点", api_key="你的key")
resp = client.chat.completions.create(
    model="你的模型",
    messages=[
        {"role": "system", "content": SYSTEM},
        {"role": "user",   "content": "设计带权限控制的 TODO 应用后端 API"},
    ],
    temperature=0.3,
)
print(resp.choices[0].message.content)   # 含四阶标记的完整推理轨迹
```

调用后可用 `core/marks.py` 的 `validate_marks()` 校验四阶结构完整性，
用 `parse_marks()` 抽取各段做后续比对或审计。

---

## 不带协议的对照基线

要验证协议是否改变了模型思维链，请用**完全相同的 user 任务**，
仅移除上面的 system 内容（或替换为中性 "你是一个有用的助手"），
两次调用独立、无共享上下文，再比对两段输出在结构/质量/Token 上的差异。

---

## 混合方案设计说明（压缩内嵌 vs 事后压缩）

独立方案（CSCD 全量输出 → 事后压缩）的缺陷：Token 已在 CSCD 阶段全额产生并计费，压缩层不感知内部重要性，递归中间态无法复用。
混合方案（递归内嵌压缩）在每阶生成时即产出 `<<<summary>>>`，压缩摘要可直接作为下一轮输入上下文，从源头节省 Token。

| 收益维度 | 独立方案 | 混合方案（协议层可达部分） |
| :--- | :--- | :--- |
| 单轮 Token | 全量产生后再压缩 | 从生成时即压缩，未全额产生 |
| 递归状态复用 | 中间态每次重生成 | summary 级联作下一轮输入 |
| 最终输出质量 | 后处理可能丢关键信息 | 关键信息经 `key_points` 保留 |

**协议层 vs 运行时边界**（重要）：
- 协议层（本模板）可约束：四阶内嵌压缩结构、递归级联比例建议、缩略图 `<<<summary>>>/<<<details>>>` 格式。
- 运行时才可兑现：KV Cache 前缀构建、显存占用下降、递归深度翻倍——这些需推理引擎/框架侧（`core/cscd.py` 程序级递归）配合，单靠注入本协议无法保证。

---

## 已知边界（诚实声明）

- 本协议当前**未**在任何真实端点完成「独立双调用思维链对照」的规模化验证。
- 早期离线仿真数据显示：在显性线索充分的任务上，协议主要表现为 **Token 净开销（约 1.3–2.1×）**，
  单次准确率增益约 0%；在低置信度/高歧义任务上观察到结构性防错收益。
- 协议是否「总能节省 Token」**未被证明**；它的价值在结构化、可审计、可插拔，
  而非性能承诺。验证数据只是效果说明，不是产品存在的必要条件。
