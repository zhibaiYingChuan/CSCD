# C-S-C-D MCP · Agent 调用引导（Host Agent 规则）

> 本文件面向**宿主 Agent 的模型**（即"负责决定是否调用 CSCD 工具"的那个模型），
> 与 `cscd://system-prompt`（面向"被推理的模型"）互补。
> 它的作用是回答一个关键问题：**什么情况下、按什么顺序调用 CSCD 工具，才能形成可追溯、可审计的结构化推理验证记录。**
>
> CSCD 调用不会改变底层模型的内部推理，也不保证结论正确；调用门禁的作用是确保结构化输出、验证证据和审计轨迹完整。

---

## 一、全局强制前置规则

### 所有任务必须先调用 `cscd_reason`

无论任务类型、复杂度、是否修改代码、是否需要工具，宿主 Agent **必须在执行任何实际工作之前**调用一次 `cscd_reason`。

适用范围包括但不限于：

- 代码编写、修改、重构和删除。
- 调试、排障、测试和验证。
- 代码审查、架构分析和安全审计。
- 文件读取、搜索、命令执行和其他工具调用。
- 文档编写、回答问题和简单事实查询。

**禁止以下顺序：**

```text
先读取文件 / 搜索代码 / 执行命令 / 修改文件
再调用 cscd_reason
```

**必须使用以下顺序：**

```text
用户任务
  → cscd_reason(完整任务描述)
  → 读取并遵循 reason / final_context
  → 执行后续工具和实际工作
  → 按需校验、抽取、压缩并交付
```

> 这是一条全局工作流门禁，不再区分“简单任务”和“复杂任务”。
> 即使 `cscd_reason` 内部将任务判定为 simple，也必须完成这次前置调用；
> simple 只表示服务端可以走方向 Y 短路，不表示宿主 Agent 可以跳过 CSCD。

### 服务端强制约束

服务端 `cscd_mcp_server.py` 已实现方案 B 调用网关（`_require_reasoned`）：

- `cscd_reason` 是所有任务的必经入口。
- 未成功调用 `cscd_reason` 时，`validate_cscd_trace`、`extract_cscd_marks`、`compress_cscd_round` 会被拒绝。
- `get_cscd_system_prompt` 和 `cscd_ledger` 仅是协议注入/账本辅助工具，不得替代 `cscd_reason`。
- `cscd_reason` 失败时，必须停止实际工作并报告失败原因；不得绕过门禁继续执行。

---

## 二、调用顺序（协议路径）

按以下顺序调用，缺一不可：

1. **`get_cscd_system_prompt`** —— 获取四阶协议文本。
   将返回的字符串作为本次推理调用的 `system` 消息注入。
2. **发起模型推理** —— 用 `user` 消息放原始任务，让模型产出四阶轨迹。
   （若走 `cscd_reason` 高层工具，则 1+2 已由服务端内部完成，见第五节。）
3. **`validate_cscd_trace`** —— 校验模型输出是否四阶完整、有序、非空。
   - 若 `ok=false` 或 `missing` 非空：**重试一次**，把校验错误回喂给模型要求补全。
4. **`extract_cscd_marks`**（可选）—— 抽取各段，做结构比对或审计。
5. **`compress_cscd_round`** —— 对轨迹做程序级压缩，用返回的 `next_round_context`
   **替代原始全量轨迹**作为最终上下文（替代式压缩，真实削减终端 Token）。
6. 以 `compress_cscd_round` 的 `next_round_context` 或模型 `COMBINE` 结论作为**最终回答**。

---

## 三、短路路径（simple 任务）

当任务被判定为 simple（复杂分析需求不强、无非可信输入）时：

- 直接调用 **`cscd_reason`**（若已配置模型端点），服务端会自动走"方向Y 短路"——
  用基线直答返回，不产生协议骨架开销，一个调用拿结论。
- 此时**无需**手动走第二节的 1–5 步。

---

## 四、响应解析约定

`cscd_reason` 返回的字段中：

| 字段 | 含义 | 使用建议 |
| :--- | :--- | :--- |
| `reason` | 终端回传文本 | 直接作为最终回答（simple 短路 = 基线文本；协议路径 = 精炼结论） |
| `final_context` | 程序级压缩后的最终输出 | 同上，二者通常一致 |
| `complexity` / `strategy` | 复杂度与推理策略 | 审计 / 元信息 |
| `marks_valid` | 轨迹四阶是否完整 | 为 `true` 说明协议结构合规 |
| `cache_hits` | 命中单原子缓存轮数 | 性能诊断 |
| `total_completion_tokens` | 累计输出 Token | Token 计量 |

---

## 五、两种接入形态的选择

- **协议插件形态（细粒度控制）**：用第二节的 1–5 步，适合需要**逐段审计 / 自定义注入**的场景。
- **推理网关形态（一个调用拿结论）**：直接用 `cscd_reason`，适合**希望引擎内部自行完成**四阶、校验、压缩、缓存、短路的场景。

> 若 `cscd_reason` 返回配置缺失错误，说明服务端未设置 `OPENAI_BASE_URL` / `OPENAI_API_KEY`，
> 请回退到协议插件形态（第二节 1–5 步）自行调用模型，或在服务端补全环境变量。
